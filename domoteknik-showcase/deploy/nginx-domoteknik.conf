server {
    listen 8081;
    listen [::]:8081;

    server_name domoteknik.belctech.com www.domoteknik.belctech.com;
    root /var/www/domoteknik;
    index index.html;

    access_log /var/log/nginx/domoteknik.access.log;
    error_log /var/log/nginx/domoteknik.error.log;

    location / {
        try_files $uri $uri/ $uri.html =404;
    }

    location ~* \.(?:css|js|jpg|jpeg|png|gif|svg|webp|ico|woff2?)$ {
        expires 30d;
        add_header Cache-Control "public, immutable";
        try_files $uri =404;
    }

    location = /index.html {
        add_header Cache-Control "no-cache";
    }

    add_header X-Content-Type-Options "nosniff" always;
    add_header Referrer-Policy "strict-origin-when-cross-origin" always;
    add_header X-Frame-Options "SAMEORIGIN" always;
}
